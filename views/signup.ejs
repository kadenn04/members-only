<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
</head>
<body>
    <h1>Sign Up</h1>
    <%- include("partials/signuperrors.ejs") %>
    <form action="/signup" method="POST">
        <p>
            <label for="firstname"> First Name </label>
            <input id="firstname" name="firstname" placeholder="John" type="text" value = "<%=locals.existingFirstname %>" />
        </p>
        <p>
            <label for="lastname"> Last Name </label>
            <input id="lastname" name="lastname" placeholder="Doe" type="text" value = "<%=locals.existingLastname %>" />
        </p>
        <p>
            <label for="username">Username (email) </label>
            <input id="username" name="username" placeholder="johndoe@gmail.com" type="username" value = "<%=locals.existingUsername %>" />
        </p>

        <p>
            <label for="password">Password</label>
            <input id="password" name="password" type="password" value = "<%=locals.existingPassword %>" />
        </p>
        <p>
            <label for="confirmPassword">Confirm Password</label>
            <input id="confirmPassword" name="confirmPassword" type="password" value = "<%=locals.existingConfirmPassword %>" />
        </p>
        <p>
            <input type="checkbox" id="isadmin" name="isadmin" value="True">
            <label for="isadmin"> Admin </label>
        </p>
        <p><button type ="submit">Sign Up</button></p>
    </form>
    <a href="/login"> Already have an account? </a>
</body>
</html>